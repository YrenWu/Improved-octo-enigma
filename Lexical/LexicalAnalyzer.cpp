/*void dump(const string str)
{
  for(size_t i = 0; i < str.size(); ++i) {
    cout << '\'' << str[i] << '\'' << ' ';
  }
  cout << endl;
}

void checkLexeme(string line)
{
  cout << line << endl;
  //dump(line);
} */
